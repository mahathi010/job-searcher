# Base URL for the backend API.
# Leave empty (or omit) in local development — Vite's dev proxy forwards /health and /v1/* to http://localhost:8000.
# Set this in production (or when running the frontend outside the Vite dev server) to the full backend origin.
# Example: VITE_API_URL=http://localhost:8000
VITE_API_URL=
